export const mapStateToProps = (state)=>({
    // pageSize:state.pageName.pageSize,
    // cateId:state.pageName.cateId,
    // type:state.pageName.type

})

export const mapDispatchToProps = ()=>({

})
